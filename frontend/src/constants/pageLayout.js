/**
 * Authenticated page body: consistent padding and vertical rhythm.
 * Use for <main> and content wrappers under PageHeader.
 */
export const PAGE_SHELL = "space-y-6 p-4 sm:p-6 lg:px-8 lg:py-6";

/** Same horizontal+vertical padding without space-y (loaders, simple placeholders). */
export const PAGE_PADDING = "p-4 sm:p-6 lg:px-8 lg:py-6";

/** Horizontal gutters aligned with page content (sticky bars, footers). */
export const PAGE_GUTTER_X = "px-4 sm:px-6 lg:px-8";
